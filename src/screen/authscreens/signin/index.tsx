import { useState } from "react";
import { View, Text } from "react-native";
import { SigninProps } from "../../../routes/types/authroutes/authscreen";

export const Signin = ({ navigation }: SigninProps) => {
 
  return (
    <View><Text >Signins</Text></View>
  )
};